/** @type {import('postcss-load-config').Config} */
const config = {
    plugins: {
      tailwindcss: {},
      autoprefixer: {}, // add autoprefixer if needed
    },
  };
  
  export default config;
  